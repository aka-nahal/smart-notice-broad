# Display launcher deps (Raspberry Pi 5 / Debian Trixie).
# Installed automatically by start.sh into backend/.venv.
#
# System packages (run once):
#   sudo apt install -y python3-venv python3-gi gir1.2-webkit2-4.1 \
#                       libgtk-3-0 nodejs npm network-manager
#
# Camera-based presence detection (optional):
#   sudo apt install -y python3-picamera2 rpicam-apps

pywebview>=5.0
screeninfo>=0.8
opencv-python>=4.8
